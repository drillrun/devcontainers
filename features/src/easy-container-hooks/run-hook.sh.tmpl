#!/usr/bin/env sh

set -eux

HOOKS=#####REPLACE######;
WORKSPACE_DIR=$(pwd);
WORKSPACE_HOOKS=${WORKSPACE_DIR}/${HOOKS};
for HOOK in $(ls ${WORKSPACE_HOOKS}); do 
    env WORKSPACE_DIR=${WORKSPACE_DIR} sh "${WORKSPACE_HOOKS}/${HOOK}"
done

